apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-bootstrap-script
  namespace: observability

data:

  bootstrap.sh: |
    <contents of bootstrap.sh>

  teams.sh: |
    <contents of teams.sh>

  users.sh: |
    <contents of users.sh>

  team-members.sh: |
    <contents of team-members.sh>

  folders.sh: |
    <contents of folders.sh>

  permissions.sh: |
    <contents of permissions.sh>